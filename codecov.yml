# Keep Codecov statuses as merge blockers.
# Use default context names so Codecov can emit both checks.
coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 1%
        informational: false
    patch:
      default:
        target: 90%
        threshold: 1%
        informational: false
