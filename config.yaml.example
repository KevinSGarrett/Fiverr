# Example override config for local experimentation.
recommendations:
  auto_generate: true
reddit:
  enabled: false            # set true when credentials are configured
  max_posts_per_seed: 25    # matches praw search limit per call
