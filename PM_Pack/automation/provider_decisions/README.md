# Provider Decisions

This directory stores provider routing decision artifacts emitted by provider governance flows.

- Keep `.gitkeep` so the directory exists on fresh clones.
- Artifact naming convention: `PROVIDER_DECISION_YYYYMMDD_HHMMSS_microseconds.json`.
- Decision records are append-only evidence and should not be edited in place.
