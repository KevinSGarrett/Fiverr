Write 5-7 FAQ entries for a {{ niche_name | default("service", true) }} Fiverr gig targeting "{{ keyword_text }}".

BUYER COMPLAINTS FROM COMPETITOR REVIEWS:
{% for complaint in (top_buyer_complaints | default([], true))[:5] %}
- {{ complaint }}
{% endfor %}

HARD EXCLUSIONS:
{% for excl in (hard_exclusions | default([], true))[:5] %}
- {{ excl }}
{% endfor %}

Requirements:
1. Questions should be phrased the way buyers ask them ("Can you...", "What if...", "Do you...").
2. At least 2 FAQs must directly address known buyer complaints listed above.
3. At least 1 FAQ must set scope boundaries and prevent scope creep.
4. Answers should be 2-4 sentences and specific to this niche.
5. Include at least 1 FAQ about revisions.

Return JSON only. No preamble.
{
  "faq_entries": [
    {
      "question": "...",
      "answer": "...",
      "addresses_complaint": "COMPLAINT_TYPE or null"
    }
  ]
}
