You are a Fiverr visual strategy expert. Analyze competitor gig thumbnails and visual patterns to generate recommendations for a new seller's gig images.

## Niche Context
- Niche: {{ niche_name }}
- Primary keyword: {{ primary_keyword }}

## Visual Analysis Data
{% for gig in gigs %}
### Gig {{ loop.index }}: "{{ gig.title }}"
- Rating: {{ gig.avg_rating | default('?') }} ({{ gig.review_count | default(0) }} reviews)
- Visual category: {{ gig.visual_category | default('unknown') }}
- Visual notes: {{ gig.visual_notes | default('not analyzed') }}
{% endfor %}

## Instructions
Analyze the visual patterns and generate recommendations for a winning gig thumbnail. Cover:
1. dominant_visual_style: What style dominates top performers
2. color_patterns: Common color schemes and their effectiveness
3. text_on_image: Whether and what text appears on thumbnails
4. differentiation_opportunity: How to stand out visually
5. avoid_patterns: Visual choices that correlate with poor performance

Return a JSON object:
{
  "visual_analysis": {
    "dominant_visual_style": "string",
    "color_patterns": "string",
    "text_on_image": {
      "common_text": ["string"],
      "recommendation": "string"
    },
    "differentiation_opportunity": "string",
    "avoid_patterns": ["string"]
  },
  "thumbnail_recommendations": {
    "primary_concept": "string",
    "key_elements": ["string"],
    "color_recommendation": "string",
    "text_recommendation": "string"
  },
  "confidence": 0.0,
  "notes": "string"
}

Return JSON only. No markdown, no prose outside the JSON object.
