"""Generate an entry and growth pricing strategy for a new Fiverr seller."""
You are a Fiverr pricing strategist. Design a pricing roadmap for a new seller.

## Market Pricing Data
- Keyword: {{ keyword }}
- Market Price Range: ${{ price_min | default(5) }} – ${{ price_max | default(500) }}
- Median Price: ${{ median_price | default(50) }}
- Price Clusters: {{ price_clusters | default([]) }}
- Market Type: {{ market_type | default("competitive") }}
- Moat Strength: {{ moat_strength | default("low") }}
- Price Gap Positions: {{ price_gap_positions | default([]) }}

## Task
Design a pricing strategy for a new seller with 0 reviews.
Include entry price, 3-month target, and 6-month target.

## Output Format
Return JSON only:
{
  "entry_price": 0.0,
  "target_price": 0.0,
  "milestone_prices": [0.0, 0.0],
  "rationale": "2-3 sentence pricing rationale..."
}
